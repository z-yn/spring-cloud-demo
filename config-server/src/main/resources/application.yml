server:
  port: 9000
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://git.oschina.net/zouyanan/config-repo.git

#security:
#  basic:
#    enabled: true               # 开启基于HTTP basic的认证
#  user:
#    name: username
#    password: password